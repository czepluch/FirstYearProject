package Controller;

import java.util.ArrayList;
import Model.*;

/*
 * Has the responsibility of converting between the data types stored in the Model
 * and data types needed by the view
 */
public class FormatConverter {

	public static int[][][] convertData(ArrayList<Data> edges) {
		// This is simply a mock implementation
		return new int[3][3][4];
	}
}
